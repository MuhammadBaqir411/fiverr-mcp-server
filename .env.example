# Transport mode (stdio or sse)
TRANSPORT=stdio

# Minimum delay between requests in seconds (default: 2, minimum: 1)
# RATE_LIMIT_DELAY=2

# Optional: proxy URL for avoiding rate limits
# PROXY_URL=http://your-proxy:8080
